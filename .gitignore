# Dependencies
node_modules/

# Build caches (the production build in /docs IS committed for GitHub Pages)
dist/
*.tsbuildinfo
.vite/

# tsc -b emit for the node-side config (not source)
vite.config.js
vite.config.d.ts

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.local

# Local-only secrets (e.g. Tavily test key) — never commit
.env
.env.*
!.env.example
# One-off local Tavily connectivity test
tavily-test.mjs

# Generated memory eval run reports (reproducible via `npm run eval`)
documentation/memory-system/eval-runs/

# Playwright TTFT bench profile
.bench-edge/
