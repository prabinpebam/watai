# Watai 0.2.2 chronology hotfix

Status: isolated and locally verified; production publication pending.

## Scope

Preserve submitted prompts during stale history reads, keep the once-assigned logical
prompt/reply timestamps through API persistence, correlate realtime snapshots with
the acknowledged assistant ID, and retain completed replies until history takes over.
Completion ownership is fenced so an old run cannot restore a cleared handoff or
remove a replacement run. Ordering comparator, ChatView layout and scroll rules are
unchanged. No historical timestamps or data are rewritten.

The frontend requires the compatible API first. Old clients remain accepted but must
reload to receive the client-side fixes. All unrelated DoD work and the Windows
continuation watchdog remain paused/disabled until user direction.

## Identity And Evidence

- Isolated predecessor: `d7ef8de025a74829fcb5cb5c1be7f679078521f6`.
  It differs from deployed frontend source `204948d15e0c58aec85ec1efa215f399eb8ddba5`
  only in its historical release receipt. No unrelated dirty candidate source entered.
- API target/runtime: Node 20. Root lockfile unchanged. Local API dev typings are
  newer than released typings; the unchanged baseline bundle reproduced byte-for-byte.
  Production package dependencies/config are retained, not copied from local tooling.
- Candidate API ZIP SHA-256:
  `66cfc35d70f4ee14431dc019de262278e3f2318cbc7c80cab6f186e22a9a6372`.
- API bundle SHA-256:
  `3fee245750ff823d466dc47a3af85619ec0d884a2022a01ae3f9a52e7246a198`.
- Only `dist/index.cjs` changed; all 16,506 other sanitized ZIP entries are identical.
- API: 76 existing focused tests and 5 service-to-worker chronology tests passed;
  API typecheck and build passed. Frontend: hook/ordering 14 tests and server-run
  9 tests passed; final production design/contrast/type/build gates passed.
- Real ChatView/Composer/useChat/runStore/serverRun, synthetic I/O only: desktop
  50 observations, mobile 49 observations, no chronology/visibility/peer-placement
  violations. History scroll anchor stayed stable. Negative old-server-order control
  detected response-before-prompt across 50 observations. External requests blocked.
- Hook tests observe every committed React render; browser capture uses DOM mutations
  and animation frames. No authenticated production chat or provider call is claimed.

## Rollback

Retain predecessor code/docs and sanitized API package SHA-256
`dfb4513697ae23782f4d9048b161a83a5f6a73037e39cea584167833f554e868`, already stored at
`deployments/sanitized/v0.2.1-dfb4513697ae23782f4d9048b161a83a5f6a73037e39cea584167833f554e868.zip`.
Never restore historical secret-bearing archives. API rollback must conditionally
replace the serving package against the hotfix ETag and restart; frontend rollback
must create a new non-force commit restoring predecessor code/docs. Keep repaired
Cosmos indexes and all consent, deletion, privacy and data ledgers unchanged.

## Manual Validation

1. Reload https://prabinpebam.github.io/watai/ and check Settings > About for 0.2.2.
2. Submit a prompt and watch continuously from pending through streaming/completion:
   the prompt must never disappear or appear after its reply.
3. Repeat after leaving the tab open, returning from background and reconnecting.
4. Scroll into older history while a reply grows; the reading position must stay put.

Publication identities, actual API/package and frontend publication times, smoke
observations and exact rollback commands must be added after successful deployment.

## First Attempt And Recovery

The API candidate package was uploaded at `2026-09-11T12:21:43Z` and restarted, but
the Node SDK package readback failed with `ECONNRESET`. No frontend was published.
The retained sanitized predecessor was conditionally restored at
`2026-09-11T12:25:20Z` (ETag `0x8DF0FFFBEF5B9FF`), restarted, and health returned
200 at `2026-09-11T12:25:42.750Z`. No data/config/index changes occurred.

A subsequent single-connection Azure CLI read of the immutable candidate succeeded:
50,859,028 bytes, ETag `0x8DF0FFF3804503A`, full SHA-256 matching the candidate above.
The corrected rollout uses this proven serial readback path, not the failed transport.
