# Watai 0.2.3: Prompt-Directed Image Transparency

Status: validated isolated candidate; production publication pending.

## Scope

Chat's image tool now exposes `background: auto | opaque | transparent` and instructs
the chat model to select transparent for requests for true alpha, a transparent
background, no background, or background removal. Transparent objects alone do not
imply a transparent background. Both generation and editing forward the validated
choice to the image endpoint and request PNG output. No background keyword parser
or extra model call is added. Default chat requests use auto.

The image adapter also supports optional compression, multipart masks, and edit
output format. Studio edits preserve their stored output format; Studio does not
use chat's semantic background selection. No new mask UI is included.

Uses the existing saved Azure resource/key and configured image deployment. The
user identified `gpt-image-2.5-sunburst`; this release does not rewrite credentials
or force another model on other users. Provider alpha output has not been tested.

## Candidate And Recovery

- Isolated predecessor/frontend: `8d56ce548c92e9f68b3522268d86d0dba2bdc9c4`.
- Current package retained locally at `.release-artifacts/image-alpha/baseline.zip`.
- Baseline SHA-256: `66cfc35d70f4ee14431dc019de262278e3f2318cbc7c80cab6f186e22a9a6372`.
- Baseline serving ETag: `"0x8DF10004C8C155E"`.
- Candidate SHA-256: `c75c8c383b4e70df0ed9f0dd3f6e5e0d51a6f79e503a7c78b176faf624832710`.
- Candidate bundle SHA-256: `116ffccb19f468c300dd01c7107ec43b12a97daf19cdacae22aa2b9451cddcf2`.
- Node 20, existing dependencies and host configuration retained.
- Exactly one ZIP entry changed, `dist/index.cjs`; 16,506 entries byte-identical.
- No app settings, data, consent, deletion, privacy, or retention ledgers changed.

## Validation

- Baseline API bundle reproduced byte-for-byte before source edits.
- Isolated candidate: 68 image/worker tests passed, including generation/edit
  background forwarding, multipart masks, PNG output, and invalid arguments.
- Main workspace: 61 image/HTTP tests, 453 chat/shared tests, API typecheck passed.
- Isolated frontend design-system, contrast, TypeScript, and production build passed.
- No live provider calls, browser evals, or alpha-pixel claims.

## Manual Validation

1. Reload https://prabinpebam.github.io/watai/ and check Settings > About for 0.2.3.
2. Confirm the image deployment is `gpt-image-2.5-sunburst` in Settings.
3. In chat ask: "Create a simple blue circle logo with a fully transparent background."
4. Download the PNG and inspect its alpha channel or composite it on two contrasting
   backgrounds. A painted checkerboard or white rectangle is a failure.
5. Attach an image and ask "Remove the background, keeping real alpha transparency."
6. Check an ordinary/explicitly opaque image request and chat prompt/reply ordering.

## Code-Only Rollback

Before any recovery require the retained baseline hash and current serving ETag.
Conditionally upload the retained ZIP to `deployments/released-package.zip`, restart
the API, and verify health. Restore predecessor frontend code/assets in a new commit
and non-force push; never reset data or restore app-settings snapshots. Exact active
ETag and publication timestamps will be recorded after production smoke.