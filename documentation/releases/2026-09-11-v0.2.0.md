# Watai 0.2.0 production milestone

Status: deployed and smoke-validated.

## Benefits

- Safer account-isolated sync, durable sends, settings revisions, run admission, dispatch, cancellation, and streaming.
- Saved-memory policy, inventory, editing, extraction, and deletion behavior is more explicit and resilient.
- Voice capture reliably stops on mute, exit, delayed permission, and device loss.
- Search and collection workflows reject stale responses and expose recovery.
- Local export/clear controls accurately describe and enforce their scope.
- Loading failures, dialogs, keyboard interaction, contrast, and narrow-screen controls are substantially improved.

## Release identity

- Frontend release: `66810d5e3056d8b1491a9abe44f956330b8d4ded`, tag `release-v0.2.0`, served from `master/docs`.
- Previous frontend: `f7dc195300c4039aae5b9a7a8fb1691327864ea1` (`master/docs`).
- Backend source: `66810d5e3056d8b1491a9abe44f956330b8d4ded`, built and remotely published from `api/`.
- Backend package: `stwataicbroocyg3omrk/deployments/released-package.zip`, 50,824,978 bytes, ETag `0x8DF0F78DB325629`, modified `2026-09-10T20:19:45Z`.
- Previous backend package: `stwataicbroocyg3omrk/deployments/released-package.zip`, baseline SHA-256 `7519a39a0c191d2a6cee8d12cccd7cb8f9bfdef345881080be4b59e76bb65e42`.
- Immutable backend rollback copy: `stwataicbroocyg3omrk/deployments/rollback/pre-v0.2.0-7519a39a.zip`, 56,419,586 bytes, ETag `0x8DF0F78C3DEA55D`.

## Production smoke

- GitHub Pages build: `built` for commit `66810d5e3056d8b1491a9abe44f956330b8d4ded`.
- Public app: HTTP 200; entry `assets/index-j5e-3che.js` HTTP 200.
- In-app release chunk: `assets/Settings-vGpG4BDi.js` HTTP 200 and contains the v0.2.0 release marker.
- API: `https://func-watai-cbroocyg3omrk.azurewebsites.net/api/health` HTTP 200.

## Manual validation

1. Sign in at `https://prabinpebam.github.io/watai/` and open Settings > About; confirm version 0.2.0 and this release note.
2. Open an existing conversation, send a message, navigate away and back, then confirm the draft/reply persists.
3. Search for text in an older message and confirm the exact message becomes visible.
4. Mute/exit Voice mode during capture and confirm recording stops without sending.
5. Open Settings > Data controls and verify export/clear descriptions match the intended local-only scope.

## Rollback

- Frontend: from the release branch run `git checkout f7dc195 -- docs`, commit the restored tree, then `git push origin HEAD:master`.
- Backend: run `./scripts/deploy-api-release.ps1 -Mode Rollback`; the script requires the retained baseline package to match its recorded SHA-256 before restoring `released-package.zip` and restarting the Function App.
- Never restore older data, settings, consent, deletion, or privacy records during code rollback.