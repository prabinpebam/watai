# Watai 0.2.1 production milestone

Status: candidate validated; production receipt completed during promotion.

## Benefit

Reconnect and offline history now converge even when delayed events share the exact cursor timestamp. Current clients also recover once from explicitly invalid legacy cursors instead of remaining stuck.

## Candidate evidence

- Root: 86 files / 504 tests passed.
- API: 84 files / 627 tests passed.
- Frontend and API production builds passed.
- Equal-time replay, stable ID de-duplication, malformed cursor signaling, and bounded client full-resync are covered directly.

## Manual validation

1. Sign in at `https://prabinpebam.github.io/watai/` and confirm Settings > About shows version 0.2.1.
2. Open the app in two browser sessions, send messages from both, and verify each session catches up after focus/reconnect.
3. Reload an older conversation and confirm no duplicate messages or terminal reply regression appears.

## Rollback

- Frontend rollback target: v0.2.0 production receipt commit `2440eb6`.
- Backend rollback target: the v0.2.0 production package retained and hash-verified immediately before v0.2.1 promotion.
- Code rollback never restores older data, consent, deletion, privacy, or cursor records.